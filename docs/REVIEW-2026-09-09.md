# Follow-up delivery — 2026-09-09

This change set includes the September 8 review fixes (#170–178), durable
atomic claims for agent confirmations (#179), optional bounded OCR (#84),
structured chunk provenance (#147), and the requested Groq provider.

Groq supports GPT-OSS 20B/120B, per-user encrypted keys, streaming, tools and
normal generation. Selection follows the published Free Plan model list;
account quotas/billing remain controlled by Groq. No live API key was supplied.
See CONFIGURATION.md for setup and the external-data disclosure.

Validation: PHP regression suite, JavaScript tests and production webpack build.
A disposable Nextcloud SQLite installation successfully enabled EVA 1.4.8,
including its new nullable provenance column. This does not establish deployment
compatibility for every database or substitute for a real Groq request.
OCR extraction uses controlled runner tests; production OCR binaries were absent.

The production Nextcloud login returned HTTP 500 while status.php returned 200.
The current operating-system user cannot read the Nextcloud error log and cannot
run occ as www-data. That incident remains unresolved and is not claimed fixed.
This PR is not a production deployment. Apply the Nextcloud app upgrade/migration
before running the new provenance queries in production.

Live Ollama integration (#134), browser coverage (#133), screenshots (#119) and
further quality evaluation (#146) are not claimed complete in this delivery.
